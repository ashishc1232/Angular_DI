import { Routes } from '@angular/router';
import { DynamicJsonComponent } from './components/dynamic-json/dynamic-json.component';

export const routes: Routes = [
    {
        component:DynamicJsonComponent,
        path:"hey"
    }
];
